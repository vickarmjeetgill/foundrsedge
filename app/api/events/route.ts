// Routes for listing events (GET) and submitting new events (POST)
// All business logic is imported from our centralized handlers file
export { getEvents as GET, createEvent as POST } from "./handlers"
