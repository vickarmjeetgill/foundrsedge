// Routes for getting details (GET), editing (PUT), or deleting (DELETE) a single event by ID
// All business logic is imported from our centralized handlers file
export { getEventById as GET, updateEvent as PUT, deleteEvent as DELETE } from "../handlers"
